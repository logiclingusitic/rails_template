<p style="color: green"><%= notice %></p>

  <%= render @order_item %>

<div>
  <%= link_to "Edit this order item", v1_client_edit_order_item_path(@order_item) %> |
  <%= link_to "Back to order items", v1_client_order_items_path %>

  <%= button_to "Destroy this order item", @order_item, method: :delete %>
</div>
